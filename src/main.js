/**
 * Main Entry Point
 * QR Box Finder Application
 */

import './style.css';
import { initApp } from './ui.js';

// Initialize the application when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  initApp();
});
